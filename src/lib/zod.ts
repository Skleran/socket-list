// import { z } from "zod";

// export const listContentSchema = z.object({
//   title: z.string().min(3, "List content must contain at least 3 characters"),
//   completed: z.boolean().default(false),
// });
